# 接口文档规范

## 一、接口文档结构

每个接口文档应包含以下部分：

```
1. 接口基本信息
2. 请求参数说明
3. 响应参数说明
4. 请求示例
5. 响应示例
6. 错误码说明
7. 注意事项
```

---

## 二、接口基本信息

### 格式示例

| 项目 | 内容 |
|------|------|
| 接口名称 | 用户登录 |
| 接口路径 | `/api/v1/user/login` |
| 请求方法 | POST |
| Content-Type | application/json |
| 是否需要认证 | 否 |

### 说明

- **接口名称**：简短清晰的功能描述
- **接口路径**：完整路径，包含版本号
- **请求方法**：GET / POST / PUT / DELETE 等
- **Content-Type**：请求体类型
- **是否需要认证**：是/否，如果需要说明认证方式

---

## 三、请求参数说明

### Query 参数示例

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| page | Integer | 否 | 页码，默认1 | 1 |
| size | Integer | 否 | 每页条数，默认10 | 10 |
| keyword | String | 否 | 搜索关键词 | test |

### Path 参数示例

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| userUid | Long | 是 | 用户UID | 10001 |

### Body 参数示例

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| username | String | 是 | 用户名 | admin |
| password | String | 是 | 密码（MD5加密） | e10adc3949ba59abbe56e057f20f883e |

### 参数类型约定

| 类型 | 说明 | 示例 |
|------|------|------|
| String | 字符串 | `"hello"` |
| Integer | 整数 | `100` |
| Long | 长整数 | `1000000001` |
| Boolean | 布尔 | `true` / `false` |
| Float/Double | 浮点数 | `3.14` |
| Date | 日期 | `"2026-05-24"` |
| DateTime | 日期时间 | `"2026-05-24 12:00:00"` |
| Array | 数组 | `[1, 2, 3]` |
| Object | 对象 | `{}` |

---

## 四、响应参数说明

### 统一响应格式

所有接口遵循统一的响应格式：

```json
{
  "code": 200,
  "message": "success",
  "data": {},
  "timestamp": 1716547200000
}
```

### 响应参数说明

| 参数名 | 类型 | 说明 |
|--------|------|------|
| code | Integer | 响应码，200表示成功 |
| message | String | 响应消息 |
| data | Object/Array | 响应数据 |
| timestamp | Long | 时间戳（毫秒） |

### 业务数据参数示例

| 参数名 | 类型 | 说明 |
|--------|------|------|
| token | String | 登录Token |
| userInfo | Object | 用户信息 |
| └── userUid | Long | 用户UID |
| └── username | String | 用户名 |
| └── nickname | String | 昵称 |

---

## 五、错误码说明

### 系统级错误码

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或Token过期 |
| 403 | 无权限访问 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 业务级错误码

| 错误码 | 说明 |
|--------|------|
| 10001 | 用户不存在 |
| 10002 | 密码错误 |
| 10003 | 用户名已存在 |
| 20001 | 游戏账号未绑定 |
| 20002 | 抽卡记录不存在 |

---

## 六、完整接口文档示例

### 接口名称：用户登录

| 项目 | 内容 |
|------|------|
| 接口路径 | `/api/v1/user/login` |
| 请求方法 | POST |
| Content-Type | application/json |
| 是否需要认证 | 否 |

### 请求参数

| 参数名 | 类型 | 必填 | 说明 | 示例 |
|--------|------|------|------|------|
| username | String | 是 | 用户名 | admin |
| password | String | 是 | 密码（MD5加密） | e10adc3949ba59abbe56e057f20f883e |

### 响应参数

| 参数名 | 类型 | 说明 |
|--------|------|------|
| token | String | 登录Token |
| userInfo | Object | 用户信息 |
| └── userUid | Long | 用户UID |
| └── username | String | 用户名 |
| └── nickname | String | 昵称 |
| └── avatarUrl | String | 头像URL |

### 请求示例

```bash
curl -X POST http://localhost:8080/api/v1/user/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "e10adc3949ba59abbe56e057f20f883e"
  }'
```

### 响应示例

#### 成功响应

```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userInfo": {
      "userUid": 10001,
      "username": "admin",
      "nickname": "管理员",
      "avatarUrl": "https://example.com/avatar.jpg"
    }
  },
  "timestamp": 1716547200000
}
```

#### 失败响应

```json
{
  "code": 10002,
  "message": "密码错误",
  "data": null,
  "timestamp": 1716547200000
}
```

### 错误码说明

| 错误码 | 说明 |
|--------|------|
| 200 | 登录成功 |
| 10001 | 用户不存在 |
| 10002 | 密码错误 |
| 400 | 请求参数错误 |

### 注意事项

1. 密码需要使用 MD5 加密后传输
2. Token 有效期为 7 天
3. 登录成功后，后续请求需要在 Header 中携带 Token：
   ```
   Authorization: Bearer {token}
   ```

---

## 七、接口文档维护

### 文档位置

- 所有接口文档统一放在 `/docs/api/` 目录下
- 按模块分类，如 `user-api.md`、`game-data-api.md`

### 文档更新时机

- 接口变更前先更新文档
- 新接口开发前先写文档
- 定期 Review 文档的准确性

### 在线文档

使用 Knife4j/Swagger 自动生成在线文档：
- 地址：`http://localhost:8080/doc.html`
- 代码中使用注解生成文档

---

## 八、Controller 注解规范

### 接口注解示例

```java
@Api(tags = "用户管理")
@RestController
@RequestMapping("/api/v1/user")
public class UserController {

    @ApiOperation("用户登录")
    @PostMapping("/login")
    public Result<LoginVO> login(@Valid @RequestBody LoginDTO loginDTO) {
        // ...
    }

    @ApiOperation("获取用户信息")
    @GetMapping("/info/{userUid}")
    public Result<UserVO> getUserInfo(
        @ApiParam("用户UID") @PathVariable Long userUid) {
        // ...
    }
}
```

### DTO/VO 注解示例

```java
@Data
@ApiModel("登录请求")
public class LoginDTO {

    @ApiModelProperty(value = "用户名", required = true, example = "admin")
    @NotBlank(message = "用户名不能为空")
    private String username;

    @ApiModelProperty(value = "密码", required = true, example = "e10adc3949ba59abbe56e057f20f883e")
    @NotBlank(message = "密码不能为空")
    private String password;
}
```
