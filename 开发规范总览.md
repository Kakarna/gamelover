# GameLover 开发规范总览

## 一、规范体系

本项目遵循以下开发规范：

| 规范文档 | 说明 |
|---------|------|
| Git分支管理规范.md | Git分支管理、代码合并流程 |
| 代码提交规范.md | Git Commit 信息格式 |
| 开发流程规范.md | 完整的开发工作流程 |
| 接口文档规范.md | API接口文档编写规范 |
| 技术选型.md | 技术栈选择和说明 |
| Nacos启动失败原因及解决方法.md | 常见问题排查 |

---

## 二、代码规范

### Java代码规范

- **代码风格**：遵循阿里巴巴Java开发规范
- **命名规范**：
  - 类名：大驼峰（PascalCase），如 `UserService`
  - 方法名：小驼峰（camelCase），如 `getUserById`
  - 常量：全大写下划线分隔，如 `MAX_SIZE`
  - 包名：全小写，如 `com.gamelover.user`
- **注释规范**：
  - 类必须有JavaDoc注释
  - 公共方法必须有JavaDoc注释
  - 复杂逻辑必须有行内注释

### 数据库设计规范

- **表名**：小写+下划线，如 `sys_user`
- **字段名**：避免使用 `id`，使用语义化名称，如 `user_uid`
- **布尔字段**：使用 `is_` 前缀，如 `is_deleted`
- **时间字段**：使用 `_time` 后缀，如 `create_time`
- **必须字段**：`create_time`、`update_time`、`is_deleted`

### 前端代码规范

- **组件命名**：大驼峰，如 `UserCard.vue`
- **文件命名**：大驼峰（组件）或小驼峰（工具函数）
- **TypeScript**：优先使用类型注解
- **代码格式化**：使用 Prettier 统一格式化

---

## 三、项目结构规范

### 后端模块结构

每个微服务模块遵循以下结构：

```
gamelover-xxx-service/
├── src/main/java/com/gamelover/xxx/
│   ├── controller/      # 控制器层
│   ├── service/         # 服务层
│   │   └── impl/        # 服务实现
│   ├── mapper/          # MyBatis Mapper
│   ├── entity/          # 实体类
│   ├── dto/             # 数据传输对象
│   ├── vo/              # 视图对象
│   ├── config/          # 配置类
│   └── XxxApplication.java
├── src/main/resources/
│   ├── application.yml
│   ├── bootstrap.yml
│   └── mapper/          # MyBatis XML
└── pom.xml
```

### 前端模块结构

```
frontend/
├── src/
│   ├── components/      # 公共组件
│   ├── views/           # 页面组件
│   ├── router/          # 路由配置
│   ├── store/           # Pinia状态管理
│   ├── api/             # API接口封装
│   ├── utils/           # 工具函数
│   ├── types/           # TypeScript类型定义
│   ├── assets/          # 静态资源
│   ├── App.vue
│   └── main.ts
└── ...
```

---

## 四、开发工具配置

### 必要插件

- **IDEA插件**：
  - Alibaba Java Coding Guidelines
  - MyBatisX
  - SonarLint
  - Rainbow Brackets

- **VS Code插件**：
  - Volar
  - ESLint
  - Prettier
  - TypeScript Vue Plugin

### 代码检查工具

- **后端**：SonarQube / Alibaba Java Coding Guidelines
- **前端**：ESLint + Prettier

---

## 五、文档编写规范

### 文档目录

所有项目文档放置在根目录下，按功能分类命名。

### 文档格式

- 统一使用 Markdown 格式
- 使用 UTF-8 编码
- 文件名使用中文，如 `开发规范总览.md`

### 文档更新原则

- 代码变更前先更新设计文档
- 接口变更前先更新接口文档
- 定期Review和更新文档

---

## 六、质量门禁

### 代码提交前检查

- [ ] 代码符合规范
- [ ] 单元测试通过
- [ ] 无编译错误
- [ ] 无严重SonarLint警告
- [ ] 相关文档已更新

### 代码合并前检查

- [ ] Code Review 通过
- [ ] 自动化测试通过
- [ ] 代码冲突已解决
- [ ] 部署文档已更新
