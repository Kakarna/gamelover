# GameLover 游戏数据聚合平台 - 开发文档

## 一、项目简介

GameLover 是一个基于微服务架构的游戏数据聚合平台，采用前后端分离设计，目前支持鸣潮（Wuthering Waves）的游戏数据查询与分析。

### 1.1 微服务架构

| 服务 | 端口 | 功能 |
|------|------|------|
| Gateway | 8080 | 网关，统一入口，路由转发 |
| User Service | 8081 | 用户模块（注册、登录、游戏账号绑定） |
| Game Data Service | 8082 | 游戏数据模块（公告、角色数据） |
| Sync Service | 8083 | 数据同步服务 |
| Nacos | 8848 | 服务注册与配置中心 |

### 1.2 技术栈

**后端**
- 框架: Spring Boot 2.7 + Spring Cloud 2021
- 服务注册/发现: Nacos
- API网关: Spring Cloud Gateway
- ORM: MyBatis-Plus
- 数据库: MySQL 8.0 + Redis + MongoDB
- 消息队列: Kafka
- 分布式追踪: Sleuth + Zipkin

**前端**
- 框架: Vue 3 + TypeScript + Vite
- UI: Naive UI（二次元风格，Persona 3主题）
- 状态管理: Pinia
- 图标: Lucide Vue

## 二、项目结构

```
GameLover/
├── gamelover-gateway/              # 网关服务 (8080)
│   └── src/main/resources/
│       └── application.yml         # 路由配置
├── gamelover-common/               # 公共模块
│   ├── result/                     # 统一返回结果
│   ├── exception/                  # 全局异常处理
│   └── config/                     # 通用配置
├── gamelover-user-service/         # 用户服务 (8081)
│   └── src/main/java/com/gamelover/user/
│       ├── controller/             # 控制器
│       ├── service/                # 业务逻辑
│       ├── mapper/                 # 数据访问
│       ├── entity/                 # 实体类
│       ├── dto/                    # 数据传输对象
│       └── vo/                     # 视图对象
├── gamelover-game-data-service/    # 游戏数据服务 (8082)
├── gamelover-sync-service/         # 同步服务 (8083)
├── frontend/                       # Vue3 前端
│   └── src/
│       ├── api/                   # API 接口定义
│       ├── stores/                # Pinia 状态管理
│       ├── views/                 # 页面组件
│       ├── router/                # 路由配置
│       ├── utils/                 # 工具函数
│       └── assets/                # 静态资源
├── nacos-config/                   # Nacos配置文件
├── docker-compose.yml              # Docker编排文件
└── docs/                          # 文档目录
```

## 三、开发进度

### 3.1 已完成 ✅

#### 数据库与基础设施
- [x] Docker 容器化部署（MySQL、MongoDB、Redis、Nacos、Kafka、Zookeeper、Zipkin）
- [x] MySQL 数据库初始化（sys_user、user_game_account 表）
- [x] MongoDB 认证配置完成
- [x] Redis 缓存服务正常运行

#### 用户模块
- [x] 注册接口 `/api/v1/user/register`
- [x] 登录接口 `/api/v1/user/login`
- [x] 获取用户信息 `/api/v1/user/info`
- [x] 更新用户信息 `/api/v1/user/update`
- [x] 修改密码 `/api/v1/user/password`
- [x] 游戏账号绑定 `/api/v1/user/game-account/bind`
- [x] 游戏账号解绑 `/api/v1/user/game-account/unbind`
- [x] 游戏账号列表 `/api/v1/user/game-account/list`

#### 游戏数据模块
- [x] 鸣潮公告数据同步（每5分钟自动执行）
- [x] 公告列表接口 `/api/v1/announcement/list`
- [x] 公告详情接口 `/api/v1/announcement/{id}`
- [x] 公告数据与图片关联（tabBanner、foldBanner）
- [x] Redis 缓存优化公告查询

#### 前端开发
- [x] 登录页面实现
- [x] 注册页面实现
- [x] 首页公告展示
- [x] 侧边栏路由导航
- [x] Token 拦截器配置
- [x] 路由守卫实现
- [x] Persona 3 风格主题（深蓝+紫色+金色）
- [x] 动态背景动画（星星、月亮、樱花）
- [x] 主题切换功能（亮/暗模式）

#### 网关配置
- [x] 路径重写配置（`/api/*` → `/api/v1/*`）
- [x] 用户服务路由
- [x] 游戏数据服务路由
- [x] 同步服务路由

### 3.2 待完善 ⏳

- [ ] 游戏账号绑定功能 UI
- [ ] 用户信息页面完善
- [ ] 角色详情页面功能
- [ ] 错误处理优化
- [ ] 单元测试覆盖

## 四、问题与解决方案

### 4.1 MongoDB 连接问题

**问题**: `Authentication failed. There was a problem connecting to localhost:27017`

**原因**:
1. Docker 数据卷中保留旧密码信息
2. 本地 MongoDB 服务占用 27017 端口
3. 连接字符串未包含认证信息

**解决方案**:
```bash
# 1. 删除旧容器和数据卷
docker rm -f gamelover-mongodb
docker volume rm gamelover_mongo_data

# 2. 停止本地 MongoDB 服务
net stop MongoDB

# 3. 重新创建容器
docker run -d --name gamelover-mongodb -p 27017:27017 mongo:latest --auth

# 4. 创建认证用户
docker exec -it gamelover-mongodb mongosh admin
db.createUser({user: "admin", pwd: "gamelover123", roles: [{role: "root", db: "admin"}]})

# 5. 更新连接字符串
mongodb://admin:gamelover123@localhost:27017/gamelover?authSource=admin
```

---

### 4.2 网关路由 404 问题

**问题**: 请求 `/api/user/register` 返回 404

**原因**: `StripPrefix=1` 导致路径不匹配后端接口

**解决方案**: 修改网关 `application.yml`，使用 `RewritePath` 替代：

```yaml
routes:
  - id: gamelover-user-service
    uri: http://localhost:8081
    predicates:
      - Path=/api/user/**
    filters:
      - RewritePath=/api/(?<segment>.*), /api/v1/${segment}
```

**路径转换**:
| 前端请求 | RewritePath | 后端期望 |
|---------|-------------|---------|
| `/api/user/register` | → `/api/v1/user/register` | ✅ |
| `/api/user/login` | → `/api/v1/user/login` | ✅ |

---

### 4.3 注册接口 500 错误

**问题**: `Column 'create_time' cannot be null`

**原因**: 实体类使用了 `@TableField(fill = FieldFill.INSERT)` 但未配置 `MetaObjectHandler`

**解决方案**: 在 `UserServiceImpl` 中手动设置时间字段：

```java
user.setCreateTime(LocalDateTime.now());
user.setUpdateTime(LocalDateTime.now());
```

---

### 4.4 前端代理配置

**问题**: 前后端分离架构中请求无法正确转发

**解决方案**: 修改 `frontend/vite.config.ts`:

```typescript
server: {
  port: 5173,
  proxy: {
    '/api': {
      target: 'http://localhost:8080',
      changeOrigin: true
    }
  }
}
```

---

### 4.5 主题切换状态丢失

**问题**: 点击主题切换按钮无反应，切换页面后主题恢复默认

**原因**: `currentTheme` 是组件内局部变量，未使用 Pinia 全局管理

**解决方案**:
1. 在 `user store` 中定义主题状态
2. 使用 `localStorage` 持久化
3. 在 `MainLayout` 中调用 `userStore.toggleTheme()`

---

### 4.6 背景动画不显示

**问题**: 星星、月亮、樱花等动画元素不可见

**原因**: z-index 层级设置不当

**解决方案**:
```css
.star-field, .moon-container, .sakura-field {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: -1;
}
```

---

### 4.7 公告数据字段问题

**问题**: 公告标题和图片获取不准确

**原因**: 接口返回字段名为 `tabTitle`、`tabBanner`、`foldBanner`，而非 `title`、`imageUrl`

**解决方案**: 更新 DTO、实体类、VO 及前后端引用，统一使用正确字段名

---

## 五、启动指南

### 5.1 Docker 服务启动

```bash
cd d:\MyTotalProject\GameLover
docker-compose up -d
```

**健康检查**:
- Nacos: http://localhost:8848/nacos (nacos/nacos)
- MySQL: localhost:3306 (gamelover/gamelover123)
- Redis: localhost:6379
- MongoDB: localhost:27017 (admin/gamelover123)
- Kafka: localhost:9092
- Zipkin: http://localhost:9411

### 5.2 后端服务启动顺序

1. **gamelover-gateway** (8080)
2. **gamelover-user-service** (8081)
3. **gamelover-game-data-service** (8082)
4. **gamelover-sync-service** (8083)

```bash
cd gamelover-gateway && mvn spring-boot:run
cd gamelover-user-service && mvn spring-boot:run
cd gamelover-game-data-service && mvn spring-boot:run
cd gamelover-sync-service && mvn spring-boot:run
```

### 5.3 前端启动

```bash
cd frontend
npm install
npm run dev
```

访问 http://localhost:5173

### 5.4 测试账号

- 用户名: `kakarna`（注意拼写，两个 r）
- 密码: `123456`

## 六、接口列表

### 6.1 用户模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 注册 | POST | `/api/user/register` | 用户注册 |
| 登录 | POST | `/api/user/login` | 用户登录 |
| 个人信息 | GET | `/api/user/info` | 获取当前用户信息 |
| 更新信息 | PUT | `/api/user/update` | 更新用户信息 |
| 修改密码 | PUT | `/api/user/password` | 修改密码 |
| 绑定游戏账号 | POST | `/api/user/game-account/bind` | 绑定游戏账号 |
| 解绑游戏账号 | DELETE | `/api/user/game-account/{bindUid}` | 解绑游戏账号 |
| 游戏账号列表 | GET | `/api/user/game-account/list` | 获取游戏账号列表 |

### 6.2 公告模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 公告列表 | GET | `/api/announcement/list` | 获取公告列表 |
| 公告详情 | GET | `/api/announcement/{id}` | 获取公告详情 |
| 手动同步 | POST | `/api/sync/announcement` | 手动触发同步 |

## 七、数据库表结构

### 7.1 sys_user (用户表)

| 字段 | 类型 | 说明 |
|------|------|------|
| user_uid | bigint | 主键，自增 |
| username | varchar(50) | 用户名，唯一 |
| password | varchar(128) | 密码（BCrypt加密） |
| nickname | varchar(50) | 昵称 |
| avatar_url | varchar(255) | 头像URL |
| email | varchar(100) | 邮箱，唯一 |
| phone | varchar(20) | 手机号，唯一 |
| register_type | varchar(20) | 注册类型 |
| last_login_time | datetime | 最后登录时间 |
| last_login_ip | varchar(50) | 最后登录IP |
| email_verified | tinyint | 邮箱是否验证 |
| status | tinyint | 账号状态（1正常/0禁用） |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |
| is_deleted | tinyint | 逻辑删除标记 |

### 7.2 user_game_account (游戏账号表)

| 字段 | 类型 | 说明 |
|------|------|------|
| bind_uid | bigint | 主键，自增 |
| user_uid | bigint | 用户ID |
| game_code | varchar(20) | 游戏代码 |
| game_uid | varchar(50) | 游戏UID |
| game_nickname | varchar(50) | 游戏昵称 |
| server_id | varchar(20) | 服务器ID |
| server_name | varchar(50) | 服务器名称 |
| auth_token | varchar(500) | 认证Token |
| auth_cookie | varchar(500) | 认证Cookie |
| is_main | tinyint | 是否主账号 |
| sync_status | tinyint | 同步状态 |
| last_sync_time | datetime | 最后同步时间 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

## 八、版本记录

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-05-27 | v1.0 | 完成用户模块基本功能 |
| 2026-05-27 | v1.1 | 完成公告同步功能 |
| 2026-05-27 | v1.2 | 完成网关路由配置 |
| 2026-05-27 | v1.3 | 修复注册接口时间戳问题 |
| 2026-05-27 | v1.4 | 整合开发文档 |

---

*最后更新: 2026-05-27*