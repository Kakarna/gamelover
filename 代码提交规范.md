# 代码提交规范

## 一、Commit Message 格式

采用 Conventional Commits 规范，格式如下：

```
<type>(<scope>): <subject>

<body>

<footer>
```

---

## 二、各部分说明

### 1. type（必需）

用于说明 commit 的类型，必须是以下之一：

| 类型 | 说明 | 示例 |
|------|------|------|
| `feat` | 新功能 | feat: 添加用户登录功能 |
| `fix` | 修复bug | fix: 修复登录失败问题 |
| `docs` | 文档更新 | docs: 更新API文档 |
| `style` | 代码格式调整（不影响代码运行） | style: 格式化代码 |
| `refactor` | 重构（既不是新增功能，也不是修复bug） | refactor: 重构用户服务 |
| `perf` | 性能优化 | perf: 优化查询性能 |
| `test` | 测试相关 | test: 添加单元测试 |
| `chore` | 构建过程或辅助工具的变动 | chore: 更新依赖版本 |
| `ci` | CI/CD 相关 | ci: 更新 GitHub Actions |

### 2. scope（可选）

用于说明 commit 影响的范围，如：

- 模块名：`user`、`gateway`、`game-data`
- 功能名：`login`、`gacha`、`announcement`
- 文件名：`pom.xml`、`README.md`

示例：
```
feat(user): 添加用户注册功能
fix(gateway): 修复路由配置问题
docs(README): 更新快速开始文档
```

### 3. subject（必需）

简短描述，不超过50个字符，要求：

- 以动词开头（使用现在时）
- 首字母小写
- 结尾不加句号

示例：
```
feat: 添加用户登录功能
fix: 修复抽卡数据查询bug
docs: 更新API文档
```

### 4. body（可选）

详细描述，可以分成多行，用于解释本次提交的：

- 为什么做这个改动
- 如何解决的问题
- 可能的影响

示例：
```
feat: 添加用户登录功能

- 实现用户名/密码登录
- 实现手机号/验证码登录
- 登录成功后返回JWT Token
```

### 5. footer（可选）

用于：

- 不兼容变更（BREAKING CHANGE）
- 关闭 Issue

示例：
```
BREAKING CHANGE: 用户API接口参数变更

旧接口：POST /api/user/login
新接口：POST /api/v1/user/login

Closes #123
```

---

## 三、完整示例

### 示例 1：新增功能

```
feat(user): 添加用户注册功能

- 实现用户名/密码注册
- 实现手机号/验证码注册
- 注册时发送验证邮件

Closes #45
```

### 示例 2：修复Bug

```
fix(game-data): 修复抽卡记录查询bug

问题：查询超过1000条记录时报错
原因：分页参数处理错误
解决：修复分页逻辑，增加参数校验

Closes #78
```

### 示例 3：文档更新

```
docs(README): 更新快速开始文档

- 添加Docker启动步骤
- 更新数据库配置说明
- 添加常见问题解答
```

### 示例 4：重构

```
refactor(common): 重构统一返回结果

- 新增ResultCode枚举类
- 简化Result对象创建
- 保持向后兼容
```

### 示例 5：性能优化

```
perf(game-data): 优化公告查询性能

- 添加Redis缓存
- 缓存时间设置为5分钟
- 查询性能提升80%
```

---

## 四、注意事项

### 1. 提交粒度

- 一个 commit 只做一件事
- 避免把不相关的改动混在一个 commit 中
- 大功能可以拆分成多个小 commit

### 2. 提交时机

- 完成一个小功能后提交
- 修复一个bug后提交
- 下班前提交当天工作
- 不要堆积大量改动再提交

### 3. 提交前检查

- [ ] 代码可以正常编译
- [ ] 单元测试通过
- [ ] 没有遗留的 TODO 或 FIXME
- [ ] 代码符合规范
- [ ] 敏感信息已移除（密码、密钥等）

---

## 五、工具配置（可选）

### 使用 Commitizen 规范化提交

安装：
```bash
npm install -g commitizen
```

使用：
```bash
git add .
git cz  # 代替 git commit
```

### 使用 commitlint 检查提交信息

配合 husky 使用，可以在提交时自动检查。

---

## 六、反例（不要这样写）

### ❌ 不好的例子

```
update  # 太简单，不知道改了什么
fix bug  # 太笼统
添加功能  # 没有英文类型前缀
feat(user): 添加用户登录功能，顺便修复了一个小bug，还改了一下文档  # 混了多个改动
```

### ✅ 好的例子

```
feat(user): 添加用户登录功能
fix(user): 修复登录时密码验证bug
docs: 更新用户登录文档
```
